/**
 * This interface represent the data of a single item in a list,
 * it's content, type and id
 */
interface ISingleItem {
  content: string;
  type: string;
  id: number;
}

export default ISingleItem;
